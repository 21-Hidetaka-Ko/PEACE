- if current_user
  - if current_user.likes.find_by(note_id: note.id)
    = link_to 'いいね！を取り消す', top_unlike_path(note.id), method: :delete, remote: true
  - else
    = link_to 'いいね！', top_like_path(note.id), method: :post, remote: true


